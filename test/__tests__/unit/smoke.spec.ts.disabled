import Game from '../src/main';

describe.skip('game starts',()=>{
    let game
    beforeAll(()=>{
       game = new Game();
    });
    it('starts and loads test level',()=>{
        expect(document.body.querySelector('canvas')).not.toBe(null);
    });
});