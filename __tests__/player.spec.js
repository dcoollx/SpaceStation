//check if canvas is added to page
/** global it jest test */
import Player from '../src/utilities/Player';

describe('basic start up',()=>{
    it('adds a canvas to the document',()=>{

         document.querySelector('canvas')
    });
   
});